{
  "name": "rearc-data-quest",
  "version": "1.0.0",
  "description": "Do. Or do not. There is no fail.",
  "main": "index.ts",
  "scripts": {
    "deploy": "rm -r dist && tsc && cp package.json dist/ && cp package-lock.json dist/ && cd dist && npm ci --production && terraform apply",
    "start": "ts-node src/lambda.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "rearc",
    "data",
    "quest"
  ],
  "author": {
    "name": "Church of Thought",
    "email": "admin@churchofthought.org",
    "url": "https://github.com/churchofthought"
  },
  "license": "ISC",
  "devDependencies": {
    "@aws-sdk/types": "^3.37.0",
    "@types/aws-lambda": "^8.10.84",
    "@types/node": "^16.10.4",
    "aws-sdk": "^2.1006.0",
    "eslint": "^8.0.0",
    "prettier": "^2.4.1",
    "ts-node": "^10.3.0",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.37.0",
    "undici": "^4.7.1"
  }
}
